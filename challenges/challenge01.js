// create a function that takes in a number and a string. If the length of the string multiplied by the number is greater than 100, alert 'Winner'
const winner = (str, num) => {
	if (str.length * num > 100) {
		alert('Winner!');
	}
};
